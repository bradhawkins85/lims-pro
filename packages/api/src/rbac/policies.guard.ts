/**
 * PoliciesGuard - Re-export of PermissionsGuard from auth module
 * This guard enforces fine-grained permissions based on action, resource, and context
 */
export { PermissionsGuard as PoliciesGuard } from '../auth/permissions.guard';
